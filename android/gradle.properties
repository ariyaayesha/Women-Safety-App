org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
gradle.user.home=/home/user/.gradle
org.gradle.caching=true
# Enable Gradle daemon (improves build speed)
org.gradle.daemon=true

# Enable parallel build
org.gradle.parallel=true

# Only configure projects that are necessary
org.gradle.configureondemand=true

# Required for Flutter + AndroidX support
android.useAndroidX=true
android.enableJetifier=true

# Optional: speeds up linting and disables analytics
android.enableR8=true
android.defaults.buildfeatures.buildconfig=true

